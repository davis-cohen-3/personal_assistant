services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: pa_agent
      POSTGRES_USER: pa_agent
      POSTGRES_PASSWORD: pa_agent
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
